# ATTENDENCE_SYSTEM
## Requirements
- Node.js 20+ (khuyên dùng LTS)
- npm

## Frontend (Vite + React)
```bash
cd frontend
npm install
npm run dev

## Docker
docker compose up --build
docker compose down

