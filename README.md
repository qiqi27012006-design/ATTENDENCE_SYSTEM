# ATTENDENCE_SYSTEM
## Requirements
- Node.js 20+ (khuyên dùng LTS)
- npm

## Frontend (Vite + React)
```bash
npm run install:all
npm run dev:all

## Docker
docker compose up --build
docker compose down

